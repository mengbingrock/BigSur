# Expo / React Native — generated and local-only, never committed.
node_modules/
.expo/
dist/
web-build/

# Native projects are generated by `expo prebuild` (Continuous Native
# Generation) via app.json + plugins/withSceneLifecycle.js. Regenerable, huge
# (Pods), and machine-specific — keep them out of git.
ios/
android/

# Metro / bundler
.metro-health-check*

# Debug
*.log
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env
.env*.local
